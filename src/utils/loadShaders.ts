// import all functions from a "index.ts" file within any "shader-modules" folder
// and concatenate the text content.
export default async function loadShaders(url: URL, exclude: boolean = false, ): Promise<string> {
    let output: string = "";

    

    return output;
}